# Backend Environment Configuration

# Database Connection (use docker-compose managed Postgres by default)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/brainbolt

# Redis Connection (use docker-compose managed Redis by default)
REDIS_URL=redis://localhost:6379

# Server Port
PORT=4000

# Node Environment
NODE_ENV=development
